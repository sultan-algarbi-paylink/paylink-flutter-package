name: paylink_payment_example
description: Demonstrates how to use the paylink_payment plugin.
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  paylink_payment:
    path: ../
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
